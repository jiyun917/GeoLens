# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# python
venv/
.venv/
.env
__pycache__

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# GeoLens specific
data/chromadb/
# PyMuPDF-based reference snapshot for byte-exact reproduction of the paper's
# headline numbers. Distributed separately (see README §6); not stored in git.
data/chromadb_pymupdf_reference/
data/uploads/
data/repos/
data/manuals.json
# CLIP image cache — regenerated from PDFs via scripts (see README).
# Excluded due to 164MB size; regeneration procedure documented for reproducibility.
data/manual_images/
# Note: data/eval/results/discarded/ IS tracked (audit trail for
# discarded replicates — required for reproducibility claim in paper).

# Internal working docs — never push (per policy discussion 2026-07-24)
NEXT_SESSION_TODO.md
